<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @photo.name %>
</p>

<p>
  <a href="#myModal" data-toggle="modal"><img src="<%= @photo.image.url(:normal) %>" /></a>
</p>

<p>
  <b>Description:</b>
  <%= @photo.description %>
</p>


<%= link_to 'Edit', edit_user_user_album_user_photo_path(@user, @album, @photo) %> |
<%= link_to 'Back', user_user_album_path(@user, @album) %>
<%= link_to 'Delete', user_user_album_path(@user, @album), :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :method => "delete", :class => "btn btn-danger" %>

<a href="#myModal" role="button" class="btn" data-toggle="modal"></a>

<!-- Modal -->
<div id="myModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Modal header</h3>
  </div>
  <div class="modal-body">
    <p>
      <img src="<%= @photo.image.url(:normal) %>" />
    </p>
  </div>
</div>